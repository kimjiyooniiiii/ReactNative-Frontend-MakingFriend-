import React, { useState } from "react";
import { View, Text } from "react-native";

const BoardList = () => {
  return (
    <View>
      <Text>BoardList</Text>
    </View>
  );
};

export default BoardList;
